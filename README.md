# TXTCREATOR - Create txt matching image

The program creates a txt file for every image file in given dataset directory. The text file is given the same name as the image.

Example:
01.png 01.txt

# Structure

/src
    __init__.py
    __main__.py
    config.py
        PACKAGE_DIR
        PROJECT_ROOT
        DATA_DIR
    main.py
        run
    processing.py
    utils.py